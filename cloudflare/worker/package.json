{
  "name": "access-control-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Access Control — Cloudflare Worker API gateway + Durable Object WebSocket hub",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "node --check src/worker.js"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
