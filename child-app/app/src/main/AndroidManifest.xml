<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ═══ Core connectivity ═══ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- ═══ Usage access (screen time / app usage) — user grants in Settings ═══ -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <!-- App list visibility on Android 11+ (parental-control core function) -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- ═══ Location — only used when parent enables monitoring AND child grants ═══ -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ═══ Foreground services (realtime link + protection enforcement) ═══ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ═══ Boot restart: restore monitoring after reboot ═══ -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- ═══ Block overlay (shows a friendly screen when a limit is reached) ═══ -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- ═══ Battery optimization exemption (reliable connection on stock Android) ═══ -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".AccessControlApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.AccessControl">

        <!-- Splash / onboarding flow. Launcher entry lives on the alias below
             so the icon can be hidden via the alias while dial codes keep working. -->
        <activity
            android:name=".onboarding.SplashActivity"
            android:exported="false"
            android:theme="@style/Theme.AccessControl.Splash" />

        <activity android:name=".onboarding.TermsActivity" android:exported="false" />
        <activity android:name=".onboarding.PermissionsActivity" android:exported="false" />
        <activity android:name=".pairing.PairingActivity" android:exported="false" />
        <activity android:name=".ui.DashboardActivity" android:exported="false" />
        <activity android:name=".ui.SettingsActivity" android:exported="false" />
        <activity android:name=".diagnostics.DiagnosticsActivity" android:exported="false" />

        <activity
            android:name=".controls.BlockActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AccessControl.Block" />

        <!-- Launcher alias: hiding the app disables THIS component only, so
             SecretCodeReceiver (dial *#*#9999#*#*) keeps working. -->
        <activity-alias
            android:name=".LauncherAlias"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:label="@string/app_name"
            android:targetActivity=".onboarding.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- ═══ Services ═══ -->
        <service
            android:name=".websocket.RealtimeService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <service
            android:name=".controls.PolicyEnforcerService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".notifications.ChildMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <service
            android:name=".notifications.ChildNotificationListener"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- ═══ Receivers ═══ -->
        <receiver
            android:name=".boot.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Diagnostic entry: dial *#*#9999#*#* (fallback *#*#1000#*#*) -->
        <receiver
            android:name=".diagnostics.SecretCodeReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.provider.Telephony.SECRET_CODE" />
                <data android:scheme="android_secret_code" android:host="9999" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.provider.Telephony.SECRET_CODE" />
                <data android:scheme="android_secret_code" android:host="1000" />
            </intent-filter>
        </receiver>

        <!-- Uninstall protection / device management (user-visible activation) -->
        <receiver
            android:name=".devicemanagement.AccessControlAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admins" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
